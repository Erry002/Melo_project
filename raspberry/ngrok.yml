version: "2"
authtoken: "your-token-here"  # Inserisci il tuo token Ngrok
console_ui: false
web_addr: localhost:4040
region: eu
log_level: warn
log_format: json

tunnels:
  web:
    addr: 80
    proto: http
  websocket:
    addr: 3001
    proto: http
    inspect: false
    schemes: 
      - https
    metadata: "app=meluccio"
    compression: true
    bandwidth_limit:
      rate: 1000000 # 1MB/s
    proxy_proto: false
    bind_tls: true
